import styled from '@emotion/styled';

const StyledPage = styled.div`
    .page {
    }
`;

export function Index() {
    /*
     * Replace the elements below with your own.
     *
     * Note: The corresponding styles are in the ./styles.css file.
     */
    return (
        <StyledPage>
            <div className="wrapper">
                <div className="container">
                    This page is unused. It should never appear. hanging onto it
                    for now as an example of emotion styling
                </div>
            </div>
        </StyledPage>
    );
}

export default Index;
